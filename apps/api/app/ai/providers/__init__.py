"""Provider adapters (TZ §4.4): app/ai/providers/<task>/ with a Protocol + implementations.

Tasks: llm (gemini, openai, mock), stt (worker, gemini, openai, mock), tts (worker, openai,
browser, mock), voice_emotion (worker, mock). Chains + registry live in `app.ai.chains`.
"""
